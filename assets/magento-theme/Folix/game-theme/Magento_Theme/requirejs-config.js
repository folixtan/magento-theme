/**
 * Folix Game Theme - RequireJS Configuration
 */
var config = {
    config: {
        mixins: {
        }
    },
    paths: {
        'folix/mobile-header': 'Magento_Theme/js/mobile-header'
    },
    shim: {
    },
    deps: [
        'folix/mobile-header'
    ]
};
