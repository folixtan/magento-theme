<?php
/**
 * Folix Game Theme - Header Search Form
 * 
 * 位置：header-wrapper 内，logo 之后
 * 选择器：.block-search-header
 */
?>
<div class="block block-search-header">
    <form class="form minisearch" action="<?= $block->escapeUrl($block->getUrl('catalogsearch/result')) ?>" method="get">
        <div class="field search">
            <div class="control">
                <input type="text"
                       class="input-text"
                       name="q"
                       value="<?= $block->escapeHtmlAttr($block->getRequest()->getParam('q')) ?>"
                       placeholder="<?= $block->escapeHtmlAttr(__('Search games, products...')) ?>"
                       maxlength="128"
                       role="combobox"
                       aria-haspopup="false"
                       aria-autocomplete="both"
                       autocomplete="off"/>
            </div>
            <div class="actions">
                <button type="submit"
                        class="action search"
                        title="<?= $block->escapeHtmlAttr(__('Search')) ?>"
                        aria-label="<?= $block->escapeHtmlAttr(__('Search')) ?>">
                    <span class="search-icon">🔍</span>
                </button>
            </div>
        </div>
    </form>
</div>
